name: Jupiter Analyze

run-name: Jupiter Analyze - ${{ inputs.job_id }}

on:
  workflow_dispatch:
    inputs:
      job_id:
        description: Jupiter analysis job ID
        required: true
        type: string
      target_minutes:
        description: Lesson duration
        required: true
        type: string
      quality:
        description: Internal Jupiter quality
        required: true
        type: choice
        options:
          - normal
          - elite

permissions:
  contents: read

concurrency:
  group: jupiter-analysis-${{ inputs.job_id }}
  cancel-in-progress: false

jobs:
  analyze:
    name: Analyze document
    runs-on: ubuntu-24.04
    timeout-minutes: 90
    env:
      JUPITER_AICREDITS_OFFLINE_CATALOG: "true"
      JUPITER_ENABLE_TOOL_STRUCTURED_OUTPUT: "false"

    steps:
      - name: Checkout public runner
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          clean: true

      - name: Generate private-core access token
        id: core-token
        uses: actions/create-github-app-token@v3
        with:
          client-id: ${{ vars.APP_CLIENT_ID }}
          private-key: ${{ secrets.APP_PRIVATE_KEY }}
          owner: akshathdubey
          repositories: |
            jupiter-core
          permission-contents: read
          permission-metadata: read

      - name: Checkout canonical Jupiter core
        uses: actions/checkout@v4
        with:
          repository: akshathdubey/jupiter-core
          token: ${{ steps.core-token.outputs.token }}
          ref: main
          path: jupiter-core
          fetch-depth: 1
          clean: true

      - name: Verify canonical core before paid work
        shell: bash
        run: |
          set -euo pipefail
          cd jupiter-core
          echo "JUPITER CORE COMMIT=$(git rev-parse HEAD)"
          grep -q '^import os$' app/intelligence/llm_gateway.py
          test -f app/visual_engine/spatial.py
          echo "CORE PREFLIGHT = OK"

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: |
            requirements.txt
            jupiter-core/requirements.txt
            jupiter-core/pyproject.toml

      - name: Verify system dependencies
        shell: bash
        run: |
          set -euo pipefail
          missing=0
          command -v pkg-config >/dev/null 2>&1 || missing=1
          pkg-config --exists cairo >/dev/null 2>&1 || missing=1
          pkg-config --exists pangocairo >/dev/null 2>&1 || missing=1
          pkg-config --exists glib-2.0 >/dev/null 2>&1 || missing=1
          if [ "$missing" -eq 1 ]; then
            sudo apt-get update -y
            sudo apt-get install -y --no-install-recommends pkg-config libcairo2-dev libpango1.0-dev libpangocairo-1.0-0 libglib2.0-dev
          fi
          pkg-config --modversion cairo
          pkg-config --modversion pangocairo
          pkg-config --modversion glib-2.0

      - name: Prepare Jupiter runtime
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p uploads jobs
          VENV_DIR="${GITHUB_WORKSPACE}/.jupiter_runtime_venv"
          if [ ! -x "${VENV_DIR}/bin/python" ]; then
            rm -rf "${VENV_DIR}"
            python -m venv "${VENV_DIR}"
            "${VENV_DIR}/bin/python" -m pip install --upgrade pip
            "${VENV_DIR}/bin/python" -m pip install -r requirements.txt
            "${VENV_DIR}/bin/python" -m pip install -r jupiter-core/requirements.txt
            "${VENV_DIR}/bin/python" -m pip install -e jupiter-core
          fi
          echo "${VENV_DIR}/bin" >> "$GITHUB_PATH"
          echo "VIRTUAL_ENV=${VENV_DIR}" >> "$GITHUB_ENV"

      - name: Zero-credit import and contract preflight
        env:
          PYTHONPATH: ${{ github.workspace }}/jupiter-core
          AICREDITS_API_KEY: ${{ secrets.AICREDITS_API_KEY }}
          AICREDITS_MODEL_NORMAL: ${{ vars.AICREDITS_MODEL_NORMAL }}
          AICREDITS_MODEL_ELITE: ${{ vars.AICREDITS_MODEL_ELITE }}
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          import os, py_compile
          files = [
              "analyze_runner.py",
              "jupiter-core/app/intelligence/llm_gateway.py",
              "jupiter-core/app/intelligence/dynamic_token_budget.py",
              "jupiter-core/app/intelligence/classifier.py",
              "jupiter-core/app/intelligence/teacher.py",
              "jupiter-core/app/intelligence/visual_designer.py",
              "jupiter-core/app/intelligence/visual_validator.py",
              "jupiter-core/app/intelligence/production_pipeline.py",
          ]
          for path in files:
              py_compile.compile(path, doraise=True)
          import requests, pydantic, pydantic_settings
          import app.visual_engine
          from app.intelligence.llm_gateway import LLMGateway
          from app.intelligence.dynamic_token_budget import ModelCatalog
          catalog = ModelCatalog(os.environ["AICREDITS_API_KEY"], "https://api.aicredits.in/v1")
          models = catalog.refresh()
          assert models
          print("ZERO-CREDIT PREFLIGHT = OK")
          print("MODELS =", ", ".join(m.id for m in models))
          PY

      - name: Verify Supabase job contract and claim workflow run
        env:
          JOB_ID: ${{ inputs.job_id }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          import os
          from supabase import create_client

          supabase = create_client(
              os.environ["SUPABASE_URL"],
              os.environ["SUPABASE_SERVICE_ROLE_KEY"],
          )
          job_id = os.environ["JOB_ID"]
          required = (
              "id,kind,status,stage,progress,quality,target_minutes,"
              "document_path,result_path,workflow_run_id"
          )
          result = (
              supabase.table("jobs")
              .select(required)
              .eq("id", job_id)
              .single()
              .execute()
          )
          job = result.data
          if not job:
              raise RuntimeError(f"Analysis job {job_id} was not found.")
          if job.get("kind") != "analysis":
              raise RuntimeError(f"Job {job_id} is not an analysis job.")
          if not job.get("document_path"):
              raise RuntimeError(f"Analysis job {job_id} has no document_path.")

          workflow_run_id = os.environ.get("GITHUB_RUN_ID")
          if workflow_run_id:
              update = (
                  supabase.table("jobs")
                  .update({"workflow_run_id": int(workflow_run_id)})
                  .eq("id", job_id)
                  .execute()
              )
              if getattr(update, "error", None):
                  raise RuntimeError(str(update.error))

          print("SUPABASE JOB CONTRACT = OK")
          print(f"JOB_ID = {job_id}")
          print(f"WORKFLOW_RUN_ID = {workflow_run_id}")
          PY

      - name: Static audit
        shell: bash
        run: python scripts/audit_jupiter_imports.py

      - name: Run Jupiter analysis
        env:
          JOB_ID: ${{ inputs.job_id }}
          TARGET_MINUTES: ${{ inputs.target_minutes }}
          QUALITY: ${{ inputs.quality }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          AICREDITS_API_KEY: ${{ secrets.AICREDITS_API_KEY }}
          AICREDITS_MODEL_NORMAL: ${{ vars.AICREDITS_MODEL_NORMAL }}
          AICREDITS_MODEL_ELITE: ${{ vars.AICREDITS_MODEL_ELITE }}
          JUPITER_AICREDITS_OFFLINE_CATALOG: "true"
          JUPITER_ENABLE_TOOL_STRUCTURED_OUTPUT: "false"
          PYTHONPATH: ${{ github.workspace }}/jupiter-core
          PYTHONUNBUFFERED: "1"
        shell: bash
        run: python -u analyze_runner.py

      - name: Upload analysis diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: jupiter-analysis-debug-${{ inputs.job_id }}
          path: |
            jobs/${{ inputs.job_id }}/
            uploads/
          if-no-files-found: warn
          retention-days: 7
